package org.firstinspires.ftc.teamcode.NextFTC.autonomous;

import com.pedropathing.geometry.Pose;

public class PoseStorage {
    // Default value in case TeleOp is run without auto
    public static Pose startingPose = new Pose(0, 0, 0);
    // Red goal starting pos -> new Pose(126.2, 119, Math.toRadians(36))
}
